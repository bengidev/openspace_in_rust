## Agent skills

### Issue tracker

GitHub (`bengidev/openspace_in_rust`). See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical labels (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context layout with `CONTEXT-MAP.md` at repo root. See `docs/agents/domain.md`.
